return {
    {
        "dhruvasagar/vim-table-mode",
        -- Load plugin only when one the following commands is executed 
        -- cmd = { "TableModeToggle", "TableModeEnable", "TableModeRealign", "Tableize" },
    }
}
